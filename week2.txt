Week2
==========================
    //Week2
    ตัวแปร
    char Sting = "Message"; <- ข้อความ
    int num = 1234; <- จำนวนเต็ม
    float dot = 1234.56; <- จำนวนจริง

    ขนาดของตัวแปรแต่ละชนิด
    int main()
    { char c;
    int i;
    float f;
    double d;
    printf("char = %d\n", sizeof(c));
    printf("int = %d\n", sizeof(i));
    printf("float = %d\n", sizeof(f));
    printf("double = %d\n", sizeof(d));
    }
    

    #include <math.h> เรียกใช้ฟังชั่นคณิตศาสตร์
    #define M_PI 3.14159265358979323846 กำหนดค่าพาย
    
    //printf 
    
   การแสดงข้อความ printf("ข้อความ %(ชนิดตัวแปร),ตัวแปร");
    
    ===========================
